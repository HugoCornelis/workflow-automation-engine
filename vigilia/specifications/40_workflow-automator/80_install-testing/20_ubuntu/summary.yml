---
description: Test of installation of vigilia and the workflow engine on Ubuntu.
documentation:
  explanation: |+2

    The workflow script enables the automation of customizable modular
    project-specific workflows consisting of shell commands.

  purpose: This module tests installation of vigilia and the workflow engine on Ubuntu.
harnessing:
  class:
    comment: Enter this container either with "docker exec -it workflow_install_testing_ubuntu_container bash".
    description: |+2
      This class instantiates a docker image based on the given docker file, then runs a container based on that image.
      
      The name of image and the name of the container are controlled from the class properties.
      
    docker:
      command_users:
        neurospaces:
          description: used for running commands outside the container
        neurospaces2:
          description: used for running commands inside the container
          is_remote: yes
      default_user: neurospaces
      dockerfile: ./vigilia/specifications/dockerfiles/Dockerfile.workflow-install-testing-ubuntu
      name_container: workflow_install_testing_ubuntu_container
      name_image: workflow_install_testing_ubuntu_image
    identifier: docker_harness_workflow_install_testing_ubuntu
    type: Heterarch::Test::ExecutionContext::Harness::Docker
name: 40_workflow-automator/80_install-testing/20_ubuntu/summary.yml
