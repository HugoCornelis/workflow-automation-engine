---
command: feature-testing-workflow features cd_tests
command_tests:
  - description: Are simple commands scheduled correctly?
    read: |
      # pwd
      #
      /home/neurospaces
      # cd bin
      #
      # pwd
      #
      /home/neurospaces/bin
      # cd ..
      #
      # pwd
      #
      /home/neurospaces
      # cd /
      #
      # pwd
      #
      /
    white_space: convert seen 0a to 0d 0a newlines
description: Are simple commands scheduled correctly?
