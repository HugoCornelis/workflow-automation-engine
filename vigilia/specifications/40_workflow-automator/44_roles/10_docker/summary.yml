---
description: Testing of Docker based roles.
harnessing:
  class:
    comment: Enter this container either with "docker exec -it workflow_docker_role_testing_container bash" or with ssh.
    description: |+2
      This class instantiates a docker image based on the given docker file, then runs a container based on that image.
      
      The name of image and the name of the container are controlled from the class properties.
      
      The class properties can include IP network information and IP address of the container.

    docker:
      command_users:
        neurospaces:
          description: used for running commands outside the container
        neurospaces2:
          description: used for running commands inside the container
          is_remote: yes
      default_user: neurospaces
      docker_in_docker:
        shared: yes
      dockerfile: ./vigilia/specifications/dockerfiles/Dockerfile.workflow-docker-role-testing
      name_container: workflow_docker_role_testing_container
      name_image: workflow_docker_role_testing_image
    identifier: docker_harness_docker_role_testing
    type: Heterarch::Test::ExecutionContext::Harness::Docker
name: 40_workflow-automator/44_roles/10_docker/summary.yml
