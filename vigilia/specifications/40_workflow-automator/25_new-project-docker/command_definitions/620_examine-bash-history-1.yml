---
command: history
command_tests:
  - comment: The bash history is populated with commands in the Dockerfile but it does not see through its `history` command, a `cat` command must be used to examine the history.
    description: What bash history is available to insert new workflows(1)?
    disabled: The bash history is populated with commands in the Dockerfile but it does not see through its `history` command, a `cat` command must be used to examine the history.
    read: |2
          1  # Preloaded workflow template commands
          2  ls -a1
          3  echo command 1
          4  echo command 2
          5  history
    white_space: convert seen 0a to 0d 0a newlines
description: What bash history is available to insert new workflows(1)?
