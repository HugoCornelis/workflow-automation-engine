---
description: Testing of the builtin grep operation
documentation:
harnessing:
  class:
    comment: Enter this container either with "docker exec -it workflow_features_installed_container bash" or with ssh.
    description: |+2
      This class instantiates a docker image based on the given docker file, then runs a container based on that image.
      
      The name of image and the name of the container are controlled from the class properties.
      
      The class properties can include IP network information and IP address of the container.

    docker:
      command_users:
        neurospaces:
          description: used for running commands outside the container
        neurospaces2:
          description: used for running commands inside the container
          is_remote: yes
      default_user: neurospaces
      dockerfile: ./vigilia/specifications/dockerfiles/Dockerfile.features-workflow-installed-testing
      name_container: workflow_features_installed_container
      name_image: workflow_features_installed_image
    identifier: docker_harness_workflow_features_installed
    type: Heterarch::Test::ExecutionContext::Harness::Docker
name: 40_workflow-automator/46_builtin/10_grep_code/summary.yml
