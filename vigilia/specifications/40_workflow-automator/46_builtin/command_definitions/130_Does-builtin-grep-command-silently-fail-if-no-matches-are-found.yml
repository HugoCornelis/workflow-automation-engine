---
command: feature-testing-workflow builtin grep_code abcdefgh
command_tests:
  - description: Does a builtin grep command on the code silently fail if no matches are found?
    read: ''
    white_space: convert seen 0a to 0d 0a newlines
description: Does a builtin grep command on the code silently fail if no matches are found?
